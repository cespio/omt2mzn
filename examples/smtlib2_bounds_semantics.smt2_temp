

( set-option :opt.priority box )

( declare-fun x (  ) Real )
( declare-fun y (  ) Real )
( declare-fun z (  ) Real )
( declare-fun t (  ) Real )

( minimize x )
( minimize y :lower ( - 0.5 ) :upper ( / 1.0 2.0 ) )
( minimize z :lower 0.0    :upper 1.0 )
( minimize t :lower 0.0    :upper 0.0 )
( maximize ( + y 1.0 ) )

( check-sat )
( get-objectives )

( exit )
