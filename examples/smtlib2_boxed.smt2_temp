

( set-option :opt.priority box )
( set-logic QF_LIA )
( declare-fun x (  ) Real )
( declare-fun y (  ) Real )
( declare-fun z (  ) Real )
( assert ( and
        ( <= 42.0 x )
        ( <= y x )
 ) )

( minimize x )
( maximize y )
( maximize z :upper 50.0 )

( check-sat )
( get-objectives )

( exit )
